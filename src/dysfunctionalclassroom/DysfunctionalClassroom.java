package dysfunctionalclassroom;
import frame.Player;
import frame.Lobby;
import io.Initializer;
public class DysfunctionalClassroom {
    public static void main(String[] args) {
        Player.Role[] game = Default.getRoles();
        Initializer init = new Initializer(0, 150, 300, 600);
        //Classroom cls = new Lobby(names, game);
        //for(int i = 1; true; i++) cls.test(i);
            
    }
    
}
